# Server
PORT=3000
NODE_ENV=development

# Database (matches docker-compose.yml)
DB_HOST=localhost
DB_PORT=5432
DB_USER=siloam
DB_PASSWORD=siloam
DB_NAME=siloam_vendor

# Auth
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=1d
