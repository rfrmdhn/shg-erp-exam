# The frontend calls the backend through the Vite dev proxy at /api (see vite.config.ts).
# No env vars are required for local dev. For a non-proxied deployment, point the
# axios baseURL in src/services/api.ts to your API origin.
